spring.application.name=Cart
spring.data.mongodb.database=cart_test
spring.data.mongodb.repositories.enabled=true
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG

# Disable Redis for integration tests (optional, can use embedded Redis if needed)
spring.cache.type=none

# Search Service Configuration (mocked in tests)
search.service.url=http://localhost:8090

# Member Service Configuration (mocked in tests)
member.service.url=http://localhost:8089

